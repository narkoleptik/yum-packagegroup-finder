This is a perl script that looks at the yum repositories that are available
to the system, queries their package groups, then prints out a list of
packages in each group with a description of each package.
